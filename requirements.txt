# langchain
# ipykernel
# python-dotenv
# langchain_community
# pypdf
# pymupdf
# bs4
# langchain-text-splitters
# langchain-openai
# chromadb
# langchain-ollama
# sentence-transformers
# langchain-huggingface
# faiss-cpu
# pinecone
# gradio
# langchain-google-genai

# Requires Python 3.11 or 3.12 (the pinned langchain/protobuf stack is not
# compatible with Python 3.14).

langchain==0.2.14
langchain-core==0.2.36
langchain-community==0.2.12
langchain-openai==0.1.22
langchain-google-genai==1.0.10
langchain-mistralai==0.1.12
langchain-anthropic==0.1.23
faiss-cpu
# Serving the Gradio UI as an ASGI app (asgi.py) on container/serverless hosts.
fastapi
# Bounded: Gradio 6 moved `theme`/`css` off the Blocks constructor. The code
# handles both majors, but an unbounded install could pick up a 7.x that does
# not.
gradio>=5,<7
tiktoken
python-dotenv
pypdf
pandas
numpy

# BERT classification (used by integrated_system.py).
# The default PyPI torch wheel for Linux bundles CUDA and installs to several
# GB. This app runs on CPU only, so take the much smaller CPU build; the +cpu
# local version exists only on PyTorch's own index.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.12.0+cpu ; sys_platform == "linux"
torch==2.12.0 ; sys_platform != "linux"
transformers
